name: Pull Request To Main

on: 
  pull_request:
    branch:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: node:23
    steps:
      - uses: actions/checkout@v3
        with:
          node-version: 23
      - run: npm ci
      
